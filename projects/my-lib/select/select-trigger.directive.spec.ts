import { SelectTriggerDirective } from './select-trigger.directive';

describe('SelectTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
